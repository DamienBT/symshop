{% extends "base.html.twig" %}

{% block title %}{% endblock %}

{% block body %}
	<h1>{{category.name}}</h1>

	{{form_start(categoryformView)}}
	{{form_row(categoryformView.name)}}
	<button type="submit" class="btn btn-info btn-lg">Editer le produit</button>
	{{form_end(categoryformView)}}

{% endblock %}
